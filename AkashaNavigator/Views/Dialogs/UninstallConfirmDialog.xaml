<helpers:AnimatedWindow x:Class="AkashaNavigator.Views.Dialogs.UninstallConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:AkashaNavigator.Helpers"
        Title="卸载插件"
        Width="420"
        Height="380"
        WindowStartupLocation="CenterOwner"
        Foreground="White"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <!-- 主容器 -->
    <Border x:Name="MainContainer" Background="#1E1E1E" BorderBrush="#333" BorderThickness="1" CornerRadius="8"
            RenderTransformOrigin="0.5,0.5" Opacity="0">
        <Border.RenderTransform>
            <ScaleTransform x:Name="ContainerScale" ScaleX="0.96" ScaleY="0.96"/>
        </Border.RenderTransform>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#252525" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="卸载插件" FontSize="13" FontWeight="SemiBold" 
                               Foreground="#EEE" VerticalAlignment="Center" Margin="16,0"/>
                    <Button Style="{StaticResource TitleBarCloseButtonStyle}"
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Click="BtnCancel_Click"/>
                </Grid>
            </Border>

            <!-- 内容区 -->
            <Grid Grid.Row="1" Margin="20,16,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 警告图标和标题 -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="⚠" FontSize="24" Foreground="#F59E0B" VerticalAlignment="Center"/>
                    <TextBlock x:Name="PluginNameText" Text="确定要卸载此插件吗？" 
                               FontSize="15" FontWeight="SemiBold" Foreground="White" 
                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>

                <!-- 警告信息（有引用时显示） -->
                <Border x:Name="WarningPanel" Grid.Row="1" Background="#3D2A1A" 
                        BorderBrush="#F59E0B" BorderThickness="1" CornerRadius="4" 
                        Padding="12" Margin="0,0,0,12" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="该插件被以下 Profile 引用：" Foreground="#FBBF24" FontSize="12"/>
                    </StackPanel>
                </Border>

                <!-- Profile 列表 -->
                <ScrollViewer x:Name="ProfileListScroller" Grid.Row="2" 
                              Style="{StaticResource DarkScrollViewer}" 
                              VerticalScrollBarVisibility="Auto"
                              Visibility="Collapsed">
                    <ItemsControl x:Name="ProfileList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#2A2A2A" CornerRadius="4" Padding="12,8" Margin="0,0,0,6">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📁" FontSize="14" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding}" Foreground="#DDD" FontSize="13" 
                                                   VerticalAlignment="Center" Margin="8,0,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- 卸载后果说明 -->
                <TextBlock x:Name="ConsequenceText" Grid.Row="3" 
                           Text="卸载后，这些 Profile 中将移除该插件的引用。" 
                           Foreground="#F87171" FontSize="12" TextWrapping="Wrap"
                           Margin="0,8,0,0" Visibility="Collapsed"/>
            </Grid>

            <!-- 底部按钮 -->
            <Border Grid.Row="2" Background="#252525" CornerRadius="0,0,8,8" Padding="20,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="1" Content="取消" Style="{StaticResource SecondaryButtonStyle}"
                            Click="BtnCancel_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnConfirm" Grid.Column="2" Content="确认卸载" 
                            Style="{StaticResource DangerButtonStyle}"
                            Click="BtnConfirm_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</helpers:AnimatedWindow>
