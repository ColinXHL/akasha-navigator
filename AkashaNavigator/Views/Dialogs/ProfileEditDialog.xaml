<helpers:AnimatedWindow x:Class="AkashaNavigator.Views.Dialogs.ProfileEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:AkashaNavigator.Helpers"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="编辑 Profile"
        Width="500"
        Height="580"
        WindowStartupLocation="CenterOwner"
        Foreground="White"
        ResizeMode="CanResize"
        MinWidth="450"
        MinHeight="480">

    <Border x:Name="MainContainer" Background="#1E1E1E" BorderBrush="#333" BorderThickness="1" CornerRadius="8"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform x:Name="ContainerScale" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#252525" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="📝 编辑 Profile" FontSize="14" FontWeight="SemiBold"
                               Foreground="#EEE" VerticalAlignment="Center" Margin="16,0"/>
                    <Button Style="{StaticResource WindowCloseButtonStyle}"
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- 内容区域（带滚动） -->
            <ScrollViewer Grid.Row="1" Style="{StaticResource DarkScrollViewer}" 
                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="24,16">
                    <!-- 页面标题 -->
                    <TextBlock Text="Profile 设置" FontSize="18" FontWeight="SemiBold"
                               Foreground="White" Margin="0,0,0,16"/>

                    <!-- 基本信息 -->
                    <TextBlock Text="基本信息" Style="{StaticResource SettingsGroupHeaderStyle}"/>
                    
                    <!-- 名称输入 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="名称" Style="{StaticResource SettingsLabelStyle}"/>
                        <TextBox Grid.Column="1" x:Name="TxtName" Style="{StaticResource DarkTextBoxStyle}"
                                 Tag="输入 Profile 名称..."
                                 Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                    <TextBlock x:Name="TxtError" Foreground="#F87171" FontSize="11"
                               Margin="80,0,0,8" Visibility="{Binding ShowError, Converter={StaticResource BoolToVisibilityConverter}}"
                               Text="{Binding ErrorMessage}"/>

                    <!-- 图标选择 -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="图标" Style="{StaticResource SettingsLabelStyle}"/>
                        <WrapPanel Grid.Column="1" x:Name="IconPanel">
                            <!-- 图标按钮将在代码中动态生成 -->
                        </WrapPanel>
                    </Grid>

                    <!-- 默认设置 -->
                    <TextBlock Text="默认设置" Style="{StaticResource SettingsGroupHeaderStyle}"/>

                    <!-- 默认 URL -->
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="默认 URL" Style="{StaticResource SettingsLabelStyle}"/>
                        <TextBox Grid.Column="1" x:Name="TxtDefaultUrl" Style="{StaticResource DarkTextBoxStyle}"
                                 Tag="留空使用应用默认值..."
                                 Text="{Binding DefaultUrl, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                    <TextBlock Foreground="#F87171" FontSize="11" Margin="80,0,0,12"
                               Visibility="{Binding UrlError, Converter={StaticResource NullToVisibilityConverter}}"
                               Text="{Binding UrlError}"/>

                    <!-- 快进秒数 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="45"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="快进秒数" Style="{StaticResource SettingsLabelStyle}"/>
                        <Slider Grid.Column="1" Style="{StaticResource DarkSliderStyle}"
                                Minimum="1" Maximum="30" TickFrequency="1" IsSnapToTickEnabled="True"
                                Value="{Binding SeekSeconds, Mode=TwoWay}"/>
                        <TextBlock Grid.Column="2" Text="{Binding SeekSeconds, StringFormat={}{0}s}"
                                   Style="{StaticResource SettingsLabelStyle}" HorizontalAlignment="Right"/>
                    </Grid>

                    <!-- 鼠标检测 -->
                    <TextBlock Text="鼠标检测" Style="{StaticResource SettingsGroupHeaderStyle}"/>
                    <TextBlock Text="为此 Profile 配置独立的鼠标检测设置，设置后将覆盖全局配置"
                               Foreground="#666" FontSize="11" Margin="0,0,0,12"/>

                    <CheckBox Style="{StaticResource DarkCheckBoxStyle}" Margin="0,0,0,12"
                              IsChecked="{Binding CursorDetectionEnabled, Mode=TwoWay}"
                              Content="启用鼠标检测"/>

                    <!-- 最低透明度 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="45"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="最低透明度" Style="{StaticResource SettingsLabelStyle}"/>
                        <Slider Grid.Column="1" Style="{StaticResource DarkSliderStyle}"
                                Minimum="0.1" Maximum="0.8"
                                Value="{Binding CursorDetectionMinOpacity, Mode=TwoWay}"
                                SmallChange="0.1" LargeChange="0.1"
                                TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                        <TextBlock Grid.Column="2" Style="{StaticResource SettingsLabelStyle}" HorizontalAlignment="Right"
                                   Text="{Binding CursorDetectionMinOpacity, StringFormat={}{0:F1}}"/>
                    </Grid>

                    <!-- 进程白名单 -->
                    <TextBlock Text="进程白名单" Style="{StaticResource SettingsLabelStyle}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBlock Text="添加需要启用鼠标检测的游戏进程"
                               Foreground="#666" FontSize="11" Margin="0,0,0,8"/>

                    <!-- 添加进程：手动输入 + 选取窗口按钮 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Style="{StaticResource DarkTextBoxStyle}"
                                 Text="{Binding NewProcessName, UpdateSourceTrigger=PropertyChanged}"
                                 Tag="输入进程名（不含 .exe）"/>
                        <Button Grid.Column="1" Content="添加" Style="{StaticResource PrimaryActionButtonStyle}"
                                Command="{Binding AddProcessCommand}"
                                Margin="4,0,0,0" Padding="12,6"/>
                        <Button x:Name="SelectWindowButton" Grid.Column="2" Content="📋 选取窗口" 
                                Style="{StaticResource ActionButtonStyle}"
                                Click="SelectWindowButton_Click"
                                Margin="4,0,0,0" Padding="10,6"/>
                        
                        <!-- 窗口选择 Popup -->
                        <Popup x:Name="ProcessSelectorPopup"
                               PlacementTarget="{Binding ElementName=SelectWindowButton}"
                               Placement="Bottom"
                               StaysOpen="False"
                               AllowsTransparency="True"
                               PopupAnimation="Slide">
                            <Border Background="#2A2A2A" BorderBrush="#444" BorderThickness="1"
                                    CornerRadius="4" Padding="4" MinWidth="280" MaxHeight="300">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- 标题和刷新按钮 -->
                                    <Grid Grid.Row="0" Margin="8,4,8,8">
                                        <TextBlock Text="运行中的窗口" Foreground="#AAA" FontSize="11"/>
                                        <Button Content="🔄" Style="{StaticResource ActionButtonStyle}"
                                                Command="{Binding RefreshProcessListCommand}"
                                                HorizontalAlignment="Right"
                                                Padding="4,2" FontSize="10" ToolTip="刷新"/>
                                    </Grid>
                                    
                                    <!-- 进程列表 -->
                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="250">
                                        <ItemsControl ItemsSource="{Binding RunningProcesses}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Style="{StaticResource SearchResultButtonStyle}"
                                                            Command="{Binding DataContext.SelectProcessFromPopupCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            HorizontalContentAlignment="Left"
                                                            Padding="8,6">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="{Binding ProcessName}" Foreground="White" FontWeight="SemiBold"/>
                                                            <TextBlock Text=" - " Foreground="#666"/>
                                                            <TextBlock Text="{Binding WindowTitle}" Foreground="#888" 
                                                                       TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                                                        </StackPanel>
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                        </Popup>
                    </Grid>

                    <!-- 已添加的白名单（Tag 样式） -->
                    <ItemsControl ItemsSource="{Binding ProcessWhitelist}" Margin="0,0,0,8">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="sys:String">
                                <Border Background="#333" CornerRadius="4" Padding="8,4" Margin="0,0,6,6">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding}" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                                        <Button Content="✕" 
                                                Command="{Binding DataContext.RemoveProcessCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent" Foreground="#888" BorderThickness="0"
                                                Padding="4,0,0,0" FontSize="10" Cursor="Hand"
                                                VerticalAlignment="Center">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <TextBlock Text="{TemplateBinding Content}" 
                                                                           Foreground="{TemplateBinding Foreground}"
                                                                           FontSize="{TemplateBinding FontSize}"
                                                                           Padding="{TemplateBinding Padding}"
                                                                           VerticalAlignment="Center"/>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Foreground" Value="#FF6B6B"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- 空白名单提示 -->
                    <TextBlock Text="尚未添加任何进程（将使用全局白名单）"
                               Foreground="#666" FontSize="11"
                               Visibility="{Binding ProcessWhitelist.Count, Converter={StaticResource ZeroToVisibleConverter}}"/>
                </StackPanel>
            </ScrollViewer>

            <!-- 底部按钮 -->
            <Border Grid.Row="2" Background="#252525" Padding="16,12" CornerRadius="0,0,8,8">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding CancelCommand}" Margin="0,0,8,0"/>
                    <Button x:Name="BtnSave" Content="保存" Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding SaveCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</helpers:AnimatedWindow>
