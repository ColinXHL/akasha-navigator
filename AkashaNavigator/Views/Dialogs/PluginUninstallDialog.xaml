<helpers:AnimatedWindow x:Class="AkashaNavigator.Views.Dialogs.PluginUninstallDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:AkashaNavigator.Helpers"
        Title="卸载 Profile"
        Width="480"
        Height="450"
        WindowStartupLocation="CenterOwner"
        Foreground="White"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <!-- 主容器 -->
    <Border x:Name="MainContainer" Background="#1E1E1E" BorderBrush="#333" BorderThickness="1" CornerRadius="8"
            RenderTransformOrigin="0.5,0.5" Opacity="0">
        <Border.RenderTransform>
            <ScaleTransform x:Name="ContainerScale" ScaleX="0.96" ScaleY="0.96"/>
        </Border.RenderTransform>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#252525" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="卸载 Profile" FontSize="13" FontWeight="SemiBold" 
                               Foreground="#EEE" VerticalAlignment="Center" Margin="16,0"/>
                    <Button Style="{StaticResource TitleBarCloseButtonStyle}"
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Click="BtnCancel_Click"/>
                </Grid>
            </Border>

            <!-- 内容区 -->
            <Grid Grid.Row="1" Margin="20,16,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Profile 名称和提示 -->
                <StackPanel Grid.Row="0" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠" FontSize="24" Foreground="#F59E0B" VerticalAlignment="Center"/>
                        <TextBlock x:Name="ProfileNameText" Text="确定要卸载此 Profile 吗？" 
                                   FontSize="15" FontWeight="SemiBold" Foreground="White" 
                                   VerticalAlignment="Center" Margin="12,0,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- 唯一插件提示 -->
                <Border Grid.Row="1" Background="#2D3748" BorderBrush="#4A5568" BorderThickness="1" 
                        CornerRadius="4" Padding="12" Margin="0,0,0,12">
                    <TextBlock Text="以下插件仅被此 Profile 使用，卸载 Profile 后可选择是否一并卸载这些插件：" 
                               Foreground="#A0AEC0" FontSize="12" TextWrapping="Wrap"/>
                </Border>

                <!-- 全选/全不选 -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                    <Button x:Name="BtnSelectAll" Content="全选" Style="{StaticResource SmallButtonStyle}"
                            Click="BtnSelectAll_Click"/>
                    <Button x:Name="BtnSelectNone" Content="全不选" Style="{StaticResource SmallButtonStyle}"
                            Click="BtnSelectNone_Click" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- 插件列表 -->
                <ScrollViewer Grid.Row="3" Style="{StaticResource DarkScrollViewer}" 
                              VerticalScrollBarVisibility="Auto" Margin="0,0,0,8">
                    <ItemsControl x:Name="PluginList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#2A2A2A" CornerRadius="4" Padding="12,10" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                  Style="{StaticResource DarkCheckBoxStyle}" VerticalAlignment="Top"
                                                  Margin="0,2,0,0"/>
                                        
                                        <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                            <TextBlock Text="{Binding Name}" Foreground="White" FontSize="13" 
                                                       FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Description}" Foreground="#888" FontSize="11" 
                                                       TextWrapping="Wrap" Margin="0,4,0,0"/>
                                            <TextBlock Foreground="#666" FontSize="10" Margin="0,4,0,0">
                                                <Run Text="ID: "/>
                                                <Run Text="{Binding PluginId}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <!-- 底部按钮 -->
            <Border Grid.Row="2" Background="#252525" CornerRadius="0,0,8,8" Padding="20,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="1" Content="取消" Style="{StaticResource SecondaryButtonStyle}"
                            Click="BtnCancel_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnConfirm" Grid.Column="2" Content="确认卸载" 
                            Style="{StaticResource DangerButtonStyle}"
                            Click="BtnConfirm_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</helpers:AnimatedWindow>
