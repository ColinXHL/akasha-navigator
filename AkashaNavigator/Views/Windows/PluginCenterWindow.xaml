<helpers:AnimatedWindow x:Class="AkashaNavigator.Views.Windows.PluginCenterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:AkashaNavigator.Helpers"
        xmlns:pages="clr-namespace:AkashaNavigator.Views.Pages"
        Title="插件中心"
        Width="900"
        Height="650"
        WindowStartupLocation="CenterScreen"
        Foreground="White"
        ResizeMode="CanResize"
        MinWidth="700"
        MinHeight="500">

    <!-- 主容器带边框 -->
    <Border x:Name="MainContainer" Background="#1E1E1E" BorderBrush="#333" BorderThickness="1" CornerRadius="8"
            RenderTransformOrigin="0.5,0.5" Opacity="0">
        <Border.RenderTransform>
            <ScaleTransform x:Name="ContainerScale" ScaleX="0.96" ScaleY="0.96"/>
        </Border.RenderTransform>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#252525" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="🔌 插件中心" FontSize="14" FontWeight="SemiBold" 
                               Foreground="#EEE" VerticalAlignment="Center" Margin="16,0"/>
                    <Button Style="{StaticResource WindowCloseButtonStyle}"
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- 主内容区 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧导航栏 -->
                <Border Grid.Column="0" Background="#252525" CornerRadius="0,0,0,8">
                    <StackPanel Margin="8,16,8,16">
                        <!-- Profile 管理分组 -->
                        <TextBlock Text="Profile 管理" Foreground="#666" FontSize="11" FontWeight="SemiBold"
                                   Margin="12,0,0,8"/>
                        <RadioButton x:Name="NavMyProfiles" Content="📋 我的 Profile" 
                                     Style="{StaticResource NavRadioButtonStyle}"
                                     IsChecked="True"
                                     Checked="NavButton_Checked"/>
                        <RadioButton x:Name="NavProfileMarket" Content="🛒 Profile 市场" 
                                     Style="{StaticResource NavRadioButtonStyle}"
                                     Checked="NavButton_Checked"/>

                        <Border Style="{StaticResource NavSeparatorStyle}"/>

                        <!-- 插件管理分组 -->
                        <TextBlock Text="插件管理" Foreground="#666" FontSize="11" FontWeight="SemiBold"
                                   Margin="12,0,0,8"/>
                        <RadioButton x:Name="NavInstalledPlugins" Content="📦 已安装插件" 
                                     Style="{StaticResource NavRadioButtonStyle}"
                                     Checked="NavButton_Checked"/>
                        <RadioButton x:Name="NavAvailablePlugins" Content="🔍 可用插件" 
                                     Style="{StaticResource NavRadioButtonStyle}"
                                     Checked="NavButton_Checked"/>
                    </StackPanel>
                </Border>

                <!-- 右侧内容区 -->
                <Border Grid.Column="1" Background="#1E1E1E" CornerRadius="0,0,8,0">
                    <Grid x:Name="ContentArea" Margin="24">
                        <!-- 我的 Profile 页面 -->
                        <pages:MyProfilesPage x:Name="MyProfilesPage" Visibility="Visible"/>
                        
                        <!-- Profile 市场页面 -->
                        <pages:ProfileMarketPage x:Name="ProfileMarketPage" Visibility="Collapsed"/>
                        
                        <!-- 已安装插件页面 -->
                        <pages:InstalledPluginsPage x:Name="InstalledPluginsPage" Visibility="Collapsed"/>
                        
                        <!-- 可用插件页面 -->
                        <pages:AvailablePluginsPage x:Name="AvailablePluginsPage" Visibility="Collapsed"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</helpers:AnimatedWindow>
